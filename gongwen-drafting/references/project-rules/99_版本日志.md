# 版本日志

本文件记录资料库结构、入口、工具说明和规则层面的稳定改动。具体 Word 成稿仍按 `0.资料索引/生成记录/成稿后复盘模板.md` 另建复盘卡；资料库体检和规范化过程记录放入 `0.资料索引/健康检查/`。

## 2026-06-15 v0.1 资料库规范化第一批

- 新增根目录入口 `00_开始使用_请先读.md`，明确人和 AI 的起读顺序。
- 新增 `0.资料索引/00-资料库规范化总览.md`，按 `T/P/K` 方法映射现有目录。
- 新增 `0.资料索引/健康检查/2026-06-15-资料库规范化日志.md`，作为本批修改日志。
- 更新 `0.资料索引/00-AI工作台.md`，增加资料库规范化入口和 P 区项目流转规则。
- 更新 `agent_handoff/AI_AGENT_START_HERE.md`，将根目录起读文件和规范化总览纳入接手顺序。
- 更新 `AGENTS.md`，补充稳定的资料库规范化与 `T/P/K` 结构规则。
- 本批不移动原始资料目录，不改写政策、模板和参考公文原文。

验证要求：

```bash
python3 -m unittest agent/test_gongwen_agent.py
python3 agent/gongwen_agent.py index --ocr --ocr-max-pages 3 --coverage-per-type 5
```

验证结果（2026-06-15 16:18 +0800）：

- `python3 -m unittest agent/test_gongwen_agent.py`：通过，运行 11 项，跳过 4 项。
- `python3 agent/gongwen_agent.py index --ocr --ocr-max-pages 3 --coverage-per-type 5`：通过，入库 45 个文件，状态均为 `ok`。
- 待确认：索引命令仍会扫描到 `agent/wecom_adapter/.venv/lib/python3.12/site-packages/docx/templates/default.docx`，属于既有索引排除范围问题，本批未改代码。

## 2026-06-17 v0.2 AI 助手迁移包刷新

- 更新迁移打包脚本，将 `00_开始使用_请先读.md` 和 `99_版本日志.md` 纳入迁移包。
- 统一接手说明、规范化总览和项目记忆中的版本日志路径为 `99_版本日志.md`。
- 明确迁移包不带 `.venv/`、`config.local.json`、运行日志、pid 文件等本机运行配置。
- 重新生成 `公文撰写Agent给AI助手迁移包.zip`。

验证结果（2026-06-17 11:23 +0800）：

- `python3 -m unittest agent/test_gongwen_agent.py`：通过，运行 11 项。
- zip 内容抽查：必需入口文件齐全，`.gongwen_agent`、`__pycache__`、`.DS_Store`、`.venv`、`config.local.json`、日志和 pid 文件均为 0 命中。

## 2026-06-18 v0.3 公文红头默认版式

- 固化“所有 Word 公文默认加红头”规则：红色发文机关标志、红色分隔线、标题位于红线下方。
- 更新固定版式脚本，核查请示默认生成红头；文号、签发人改为可选字段，有材料时写入，无材料时不编造、不留空项。
- 更新固定版式说明、AI handoff skill、乌兰察布模板内化和相关文种手册，清理旧的“不默认加红头”执行口径。
- 重新内化 `固定版式——文物保护许可核查请示格式范本.docx`，使 Markdown/JSON 记录包含红头段落。
- 2026-06-18 16:00 修正：红色分隔线改为连续粗段落边框，不再用长横线字符拼接；红头固定保留文号/签发人行。

验证结果（2026-06-18 15:38 +0800）：

- `python3 -m py_compile agent/gongwen_format.py agent/make_hecha_docx.py agent/check_hecha_docx.py`：通过。
- `python3 agent/make_hecha_docx.py agent/sample_hecha_project.json --out /tmp/核查请示-红头样例.docx --audit` 和 `python3 agent/check_hecha_docx.py /tmp/核查请示-红头样例.docx`：通过。
- `python3 -m unittest agent/test_gongwen_agent.py`：通过，运行 11 项。
- 已渲染样例 DOCX 为 2 页 PNG 检查：红头、红线和标题位置稳定；当前 LibreOffice 环境对 `仿宋_GB2312` 仍有字体匹配差异，属于既有渲染环境问题。

## 2026-06-22 v0.4 接入 docformat-gui GB/T 格式引擎

- 新增 `agent/vendor/docformat_engine/`，内置 `KaguraNanaga/docformat-gui` 的核心格式化引擎快照，并保留 MIT 许可证和上游 README。
- 新增 `agent/format_existing_docx.py`，用于给已经写好的 `.docx` 公文执行与上游 GUI 一致的“智能一键处理”：先修标点，再按上游 `official` 预设调格式；该预设对应 GUI 中的 `GB/T 公文标准`。
- 明确边界：既有成稿通用调格式以 `docformat-gui` GB/T 标准为准；资料库原有 `gongwen_format.py` 保留为核查请示等专项生成工具，不覆盖通用调格式入口。

验证要求：

```bash
python3 -m py_compile agent/format_existing_docx.py agent/vendor/docformat_engine/scripts/formatter.py agent/vendor/docformat_engine/scripts/punctuation.py agent/vendor/docformat_engine/scripts/analyzer.py
python3 agent/format_existing_docx.py /tmp/gongwen-format-sample.docx --overwrite
python3 agent/format_existing_docx.py /tmp/gongwen-format-sample.docx --mode diagnose
```

验证结果（2026-06-22 13:50 +0800）：

- 新入口和 vendored 引擎编译通过。
- 临时 DOCX 经默认智能模式处理后，英文冒号、逗号、句号已修为中文标点，日期由 `2026.6.22` 标准化为 `2026年6月22日`，并生成 `/tmp/gongwen-format-sample_processed.docx`。
- `python3 -m unittest agent/test_gongwen_agent.py`：通过，运行 11 项。

## 2026-06-22 v0.5 公文写作最终交付链路

- 将 `agent/gongwen_agent.py` 的写作交付链路改为：生成正文文本和内部临时 Word 后，自动调用 `docformat-gui` GB/T 公文标准智能处理。
- 桌面最终只保留一份已调好格式的 `.docx`；Markdown 草稿和未格式化 Word 仅作为临时过程文件，不作为交付物。
- Web 表单结果页只提供格式化 Word 下载，不再提供 Markdown 草稿链接。
- 企微本地生成路径同步调用同一格式化函数。

验证结果（2026-06-22 13:53 +0800）：

- `python3 -m py_compile agent/gongwen_agent.py agent/format_existing_docx.py agent/wecom_adapter/wecom_gongwen_bridge.py`：通过。
- `python3 -m unittest agent/test_gongwen_agent.py`：通过，运行 11 项。
- 使用临时 HOME 执行 `python3 agent/gongwen_agent.py draft /tmp/gongwen-project.json`：临时桌面仅生成 1 个最终 `.docx`，`markdown_path` 为空。

## 2026-06-22 v0.6 迁移包同步最终格式化链路

- 更新 `agent_handoff/AI_AGENT_START_HERE.md` 和 `agent_handoff/PROJECT_MEMORY_EXPORT.md`，将最终交付链路改为“正文文本和内部临时 Word -> `docformat-gui` GB/T 公文标准 -> 桌面最终 `.docx`”。
- 明确迁移包必须包含 `agent/format_existing_docx.py` 和 `agent/vendor/docformat_engine/`，新电脑不得漏迁格式化引擎。
- 更新 `agent/迁移打包工具/scripts/build_ai_handoff_package.py`，新增必备文件检查，缺少格式化入口或 vendored 引擎时直接停止打包。
- 重新生成 `公文撰写Agent给AI助手迁移包.zip`。

验证结果（2026-06-22 13:59 +0800）：

- `python3 -m py_compile agent/gongwen_agent.py agent/format_existing_docx.py agent/迁移打包工具/scripts/build_ai_handoff_package.py`：通过。
- `python3 -m unittest agent/test_gongwen_agent.py`：通过，运行 11 项。
- 迁移包重建并抽查：`agent/format_existing_docx.py`、`agent/vendor/docformat_engine/scripts/formatter.py`、`agent/vendor/docformat_engine/LICENSE`、`agent_handoff/AI_AGENT_START_HERE.md`、`agent_handoff/PROJECT_MEMORY_EXPORT.md` 均在 zip 内；`.venv`、`__pycache__`、`config.local.json`、日志和 pid 文件未打入。

## 2026-06-23 v0.7 公文撰写总流程 skill

- 新增 `agent_handoff/skills/gongwen-drafting/`，作为公文撰写总流程 skill。
- skill 采用 `SKILL.md + references/ + scripts/ + agents/openai.yaml` 结构：主文件负责触发、路由和总流程；`references/` 拆分文种路由、写作边界和 Word 输出规则；`scripts/check_gongwen_workspace.py` 做轻量健康检查。
- 更新 `AGENTS.md`、`agent_handoff/AI_AGENT_START_HERE.md` 和 `agent_handoff/PROJECT_MEMORY_EXPORT.md`，明确总流程 skill 与核查请示专项 skill 的分工。

验证要求：

```bash
python3 /Users/drevan01/.codex/skills/.system/skill-creator/scripts/quick_validate.py agent_handoff/skills/gongwen-drafting
python3 agent_handoff/skills/gongwen-drafting/scripts/check_gongwen_workspace.py
python3 -m py_compile agent_handoff/skills/gongwen-drafting/scripts/check_gongwen_workspace.py
```

验证结果（2026-06-23 11:58 +0800）：

- `check_gongwen_workspace.py`：通过。
- `python3 -m py_compile agent_handoff/skills/gongwen-drafting/scripts/check_gongwen_workspace.py`：通过。
- 手动结构校验：通过，确认 `SKILL.md` frontmatter、`references/`、`scripts/` 和 `agents/openai.yaml` 齐全且无 TODO。
- 官方 `quick_validate.py`：当前系统 Python 和 Codex bundled Python 均缺少 `yaml` 模块，未能运行；本次未为校验安装新依赖。

## 2026-06-23 v0.8 PyYAML 依赖补齐与迁移包重建

- 在系统 Python 和 Codex bundled Python 中安装 `PyYAML 6.0.3`，用于运行官方 skill 校验脚本 `quick_validate.py`。
- 更新 `agent/迁移打包工具/scripts/build_ai_handoff_package.py`，把 `gongwen-drafting` 总流程 skill 及 `gongwen-hecha-fixed-format` 专项 skill 加入迁移包必需文件校验。
- 更新 `agent_handoff/requirements.txt` 和 `agent/迁移打包工具/requirements.txt`，补充 `PyYAML>=6.0.3`。
- 重新生成 `公文撰写Agent给AI助手迁移包.zip`。

验证结果（2026-06-23 13:18 +0800）：

- 系统 Python 与 Codex bundled Python 均可导入 `yaml`，版本为 `6.0.3`。
- 官方 `quick_validate.py agent_handoff/skills/gongwen-drafting` 在两个 Python 环境下均通过，输出 `Skill is valid!`。
- `check_gongwen_workspace.py` 通过。
- `python3 -m unittest agent/test_gongwen_agent.py` 通过，运行 11 项。
- 迁移包已重建；zip 抽查确认 `gongwen-drafting`、`gongwen-hecha-fixed-format`、`agent/format_existing_docx.py` 和 `agent/vendor/docformat_engine/scripts/formatter.py` 均在包内，`__pycache__`、`.DS_Store`、`.venv`、`config.local.json`、`.gongwen_agent` 均为 0 命中。

## 2026-06-23 v0.9 自包含 skill 迁移包

- 修正迁移包边界：后续 `公文撰写Agent给AI助手迁移包.zip` 本身就是一个自包含 `gongwen-drafting/` skill 文件夹，不再是整库备份式迁移包，也不是只含规则的薄 skill。
- `gongwen-drafting/references/` 内置项目规则、AI 接手说明、资料索引、文种手册、模板内化、文件卡片、健康检查等可读资料。
- `gongwen-drafting/assets/` 内置政策规范、Word 模板和历史参考公文：对应原 `1.政策法规与规范/`、`2.公文模板/`、`3.公文参考/`。
- `gongwen-drafting/scripts/` 内置轻量自检脚本和原 `agent/` 工具链副本。
- 更新 `SKILL.md`、`references/resource-map.md`、`AGENTS.md`、`agent_handoff/AI_AGENT_START_HERE.md`、`agent_handoff/PROJECT_MEMORY_EXPORT.md` 和迁移打包脚本，统一“迁移包即自包含 skill”的口径。

验证结果（2026-06-23 13:26 +0800）：

- 官方 `quick_validate.py agent_handoff/skills/gongwen-drafting` 通过，输出 `Skill is valid!`。
- `python3 scripts/check_gongwen_workspace.py` 在 `gongwen-drafting/` skill 目录内通过。
- `python3 agent_handoff/skills/gongwen-drafting/scripts/check_gongwen_workspace.py` 在完整资料库根目录内通过。
- `python3 -m py_compile agent_handoff/skills/gongwen-drafting/scripts/check_gongwen_workspace.py agent/迁移打包工具/scripts/build_ai_handoff_package.py` 通过。
- `python3 -m unittest agent/test_gongwen_agent.py` 通过，运行 11 项。
- 迁移包已重建；zip 根目录为 `gongwen-drafting/`，抽查确认 `assets/templates`、`assets/reference-documents`、`assets/policies-and-standards`、`references/knowledge-index`、`scripts/gongwen-agent` 均在包内；`__pycache__`、`.DS_Store`、`.WeDrive`、`.venv`、`config.local.json`、`.gongwen_agent` 均为 0 命中。

## 2026-06-23 v0.10 gongwen-drafting skill 命名与一键安装

- 对外交付包改名为 `gongwen-drafting.zip`，与 skill 名和内部根目录 `gongwen-drafting/` 保持一致；中文显示名为“公文写作”。
- 新增 `scripts/install_environment.sh`，用于在新 Mac 一键安装基础运行环境：`python-docx`、`PyYAML`、`poppler`，以及默认的 `tesseract` 和中文语言包。
- 新增 `scripts/check_environment.py`，用于检查 Python 依赖、PDF 工具、OCR 工具、内置格式化脚本和模板资源。
- 更新 `SKILL.md`、`references/resource-map.md`、`references/word-output.md`、迁移打包脚本和接手说明。

## 2026-06-23 v0.11 skill 文件扩展名

- 对外交付文件改为 `gongwen-drafting.skill`，与 skill 名、内部根目录 `gongwen-drafting/` 和常见 skill 包扩展名保持一致。
- `.skill` 文件仍采用 zip 容器结构，根目录只包含 `gongwen-drafting/` 一个 skill 文件夹。

验证结果（2026-06-23 14:14 +0800）：

- 官方 `quick_validate.py agent_handoff/skills/gongwen-drafting` 通过。
- skill 自检、环境检查和安装脚本语法检查均通过。
- `gongwen-drafting.skill` 已生成，容器根目录仅为 `gongwen-drafting/`，必需文件齐全，缓存、本机配置和系统垃圾均未打入。

## 2026-06-24 v0.12 库伦旗文物核查请示模板

- 新增 `2.公文模板/库伦旗/模板——库伦旗文物核查请示.docx`，并生成对应模板内化 Markdown/JSON。
- 更新 `0.资料索引/02-模板索引.md`、`0.资料索引/文种手册/文物保护许可申请-作战手册.md` 和 `AGENTS.md`，明确库伦旗项目由库伦旗文化和旅游局向通辽市文化旅游广电局报送文物核查请示时，优先按库伦旗模板口径处理。
- 同步更新 `agent_handoff/skills/gongwen-drafting/` 中的模板资产和知识索引副本，避免跨设备迁移时丢失库伦旗模板。

验证结果（2026-06-24 16:06 +0800）：

- `pdftotext` 可抽取来源 PDF 正文；已渲染 PDF 页面核对红头、标题、正文和附件版式。
- `python3 agent/internalize_word_templates.py` 已重新生成模板内化总览、manifest 和库伦旗模板 Markdown/JSON。
- `rg` 检查确认库伦旗模板已进入主资料库索引和 `gongwen-drafting` skill 副本。

## 2026-06-24 v0.13 地区模板路由门禁

- 新增 `agent/check_template_route.py`，用于在生成前按“地区+文种”读取当前模板索引，判断应走地方模板、通用模板还是未知路线。
- 当索引中存在地方模板而计划走通用模板，或计划模板未命中地方模板候选时，脚本以非 0 状态退出，避免跳过地方模板。
- 同步新增 skill 包内置脚本 `scripts/gongwen-agent/check_template_route.py`，并更新 `SKILL.md`、`references/routing.md`、`AGENTS.md`、`agent/README.md`。
- 将门禁脚本加入 skill 自检和迁移打包必需清单；缺少脚本时打包失败。
- 重新生成 `gongwen-drafting.skill`。

验证结果（2026-06-24 18:18 +0800）：

- `python3 -m py_compile agent/check_template_route.py agent_handoff/skills/gongwen-drafting/scripts/gongwen-agent/check_template_route.py` 通过。
- `python3 agent/check_template_route.py --region 库伦旗 --doc-type 文物核查请示` 返回 `local`，命中“库伦旗文物核查请示”。
- `python3 agent/check_template_route.py --region 库伦旗 --doc-type 文物核查请示 --planned-route generic` 以退出码 2 拦截。
- `python3 agent/check_template_route.py --region 准格尔旗 --doc-type 文物核查请示` 当前返回 `generic`。
- 官方 skill 校验和工作区自检通过；`gongwen-drafting.skill` 已重建并抽查包含门禁脚本及规则说明。

## 2026-06-24 v0.14 移除 docformat 外部格式化链路

- 按用户反馈，移除与原固定版式规则冲突的 `docformat-gui` 集成，不再使用外部一键格式化工具覆盖资料库既有版式。
- 删除 `agent/format_existing_docx.py`、`agent/vendor/docformat_engine/` 及 skill 包内对应副本；`agent/gongwen_agent.py` 改为直接调用资料库内 Word 生成能力输出桌面 `.docx` 成稿。
- 更新 `AGENTS.md`、AI 接手说明、项目记忆导出、Word 输出说明、工具说明和固定版式说明，统一为“文种手册 + 地区模板 + 固定版式脚本”交付链路。
- 打包和自检脚本不再要求旧格式化入口，改为检查 `gongwen_format.py`、`make_hecha_docx.py`、`check_hecha_docx.py` 和 `check_template_route.py`。
- 重新生成 `gongwen-drafting.skill`。

验证结果（2026-06-24 18:12 +0800）：

- `python3 -m py_compile` 覆盖主库和 skill 副本中的生成、固定版式、路由、自检和打包脚本：通过。
- `python3 -m unittest agent/test_gongwen_agent.py` 通过，运行 11 项。
- 工作区自检、环境检查和官方 `quick_validate.py agent_handoff/skills/gongwen-drafting` 均通过。
- `gongwen-drafting.skill` 已重建；zip 抽查确认 `format_existing_docx.py`、`docformat_engine` 和 `vendor` 为 0 命中，固定版式脚本和地区模板路由门禁均在包内。
- 库伦旗文物核查请示路由仍返回 `local`，计划走通用模板时继续以退出码 2 拦截。

## 2026-06-25 v0.15 市级文物调查报告模板

- 新增并内化 `2.公文模板/模板——市级调查报告——长呼复线大东管道联络线项目文物调查报告20260402.docx`，作为鄂尔多斯市文物考古研究院市级文物调查报告优先模板。
- 更新 `AGENTS.md`、文物调查报告作战手册、模板索引、文种索引、文件卡片和 `PROJECT_MEMORY_EXPORT.md`，明确市级调查报告走封面、目录、章节式长报告模板；自治区函式报告仍走自治区调查报告模板。
- 修正 `agent/internalize_word_templates.py` 及 skill 副本：文物调查报告识别词增加“文物调查报告”“市级调查报告”，并跳过 Word 生成的 `.~` 临时文件。
- 同步更新 `agent_handoff/skills/gongwen-drafting/` 的模板资产、模板内化、项目规则副本和路由说明，并重建 `gongwen-drafting.skill`。

验证结果（2026-06-25 13:30 +0800）：

- `python3 agent/internalize_word_templates.py` 已成功识别新模板为“文物调查报告”。
- `python3 agent/check_template_route.py --region 鄂尔多斯市 --doc-type 文物调查报告` 命中市级调查报告模板。
- `python3 /Users/drevan01/.codex/skills/.system/skill-creator/scripts/quick_validate.py agent_handoff/skills/gongwen-drafting` 通过；`gongwen-drafting.skill` 已重建并抽查包含市级模板及内化文件。

## 2026-06-25 v0.16 市级调查报告替换规则复盘

- 对比【市级】益民成稿、市级模板和 AI 生成版本后，补充 `文物调查报告-作战手册` 的市级模板替换规则。
- 明确市级报告应保留封面、目录、章节、调查表、照片、坐标表骨架，按槽位替换项目事实；不把自治区核查报告整页附件或审批结论机械搬入市级调查报告。
- 明确封面/落款联合单位、目录页码、调查表 9 个字段、照片位、坐标表和 `XXXX` 残留检查要求。

## 2026-06-26 v1.0 双 skill 稳定版

- 将 `gongwen-drafting.skill` 与 `gongwen-survey-report.skill` 定为 v1.0 稳定版。
- `gongwen-drafting` 负责请示、函、许可、备案、验收、开工等公文流程；`gongwen-survey-report` 负责文物调查报告、市级调查报告、自治区调查报告等报告类流程。
- 固定版式已同步到红头默认版式，包含发文机关标志、文号/签发人行、红色分隔线、标题位置、附件悬挂缩进和落款/日期对齐提示。
- 两个 skill 均以 `.skill` 作为可迁移交付单元，默认桌面 Word 交付，保持模板忠实、文种路由、缺项提示和边界校验。

验证结果（2026-06-26 11:00 +0800）：

- `gongwen-drafting` 工作区自检通过，`gongwen-survey-report` skill 自检通过。
- `python3 -m py_compile` 覆盖两个 skill 的自检脚本和地区模板路由门禁脚本：通过。
- 官方 `quick_validate.py` 分别校验 `agent_handoff/skills/gongwen-drafting` 与 `agent_handoff/skills/gongwen-survey-report`：均通过。
- 固定版式范本已确认同步为 18 段，末尾包含“发文单位名称”“（与日期中心对齐确定缩进量）”“XXXX年X月X日”。
- 已重新生成 `gongwen-drafting.skill` 与 `gongwen-survey-report.skill`，包内根目录唯一，未包含 `.DS_Store`、`__pycache__`、`.pyc`、本机缓存或本机配置。

## 2026-06-29 v1.1 考古勘探意见书标题与模板句式

- 更新 `海南区觉海寺改扩建配套设施改造项目考古勘探项目验收意见书.docx` 模板，删除验收意见段中“XX公司对项目进行了考古勘探工作，”这类重复句。
- 同步模板内化 Markdown/JSON，便于后续检索时读取到删除后的正文骨架。
- 新增写作规则：鄂尔多斯项目生成考古勘探意见书本体时，标题统一写“考古勘探项目检查意见书”，不得写“考古勘探项目验收意见书”；开工请示附件仍按“检查意见书”口径。

验证结果（2026-06-29 13:39 +0800）：

- Word 模板已渲染为 2 页 PNG 并检查，删句后验收意见段衔接正常，表格边框、签字区和日期区无重叠或截断。
- `python-docx` 抽取确认模板中已不含“对项目进行了考古勘探工作”目标句。
- JSON 内化文件通过 `python3 -m json.tool` 校验；新增“考古勘探项目检查意见书”规则可在 `drafting-rules.md`、`routing.md`、`AGENTS.md` 和开工请示手册中检索。
- `python3 -m py_compile`、`python3 -m unittest discover -s scripts/gongwen-agent -p 'test_*.py'`、`python3 scripts/check_gongwen_workspace.py` 均通过。

## 2026-06-29 v1.2 考古勘探意见书勘探实施情况占位

- 更新考古勘探项目验收/检查意见书模板，将验收意见段中固定的“{{项目承担单位}}对项目可勘探区域进行了勘探工作，勘探面积{{项目面积}}，布设{{探孔间距}}探孔。”替换为整句占位 `{{勘探实施情况}}`。
- 新增写作规则：项目全部区域均已勘探时，不写“可勘探区域”；仅部分区域可勘探时，才写“可勘探区域”，并写明不可勘探区域范围及不可勘探原因；涉及重点勘探时，必须写明重点勘探孔距。
- 同步修正海南区觉海寺旧模板及模板内化中“对项目可勘探区域进行了勘探工作”的误导表述。

验证结果（2026-06-29 15:46 +0800）：

- `python-docx` 抽取模板正文确认新模板已不再硬编码“可勘探区域”，并含 `{{勘探实施情况}}` 占位。
- 固定正文“专家组向项目方详细了解……”和“注：本验收意见只作为……”仍保留。

## 2026-06-29 v1.3 锡林郭勒盟/西乌珠穆沁旗文物调查事宜请示模板

- 新增 `assets/templates/锡林郭勒盟/模板——锡林郭勒盟西乌珠穆沁旗文物调查事宜请示.docx`，作为锡盟、西乌项目文物保护许可申请、文物核查请示或申请办理文物调查事宜的地方模板。
- 更新 `internalize_word_templates.py`，使其在自包含 skill 结构下读取 `assets/templates` 和 `assets/fixed-format`，并把“文物调查事宜/办理文物调查”归入文物保护许可申请。
- 更新模板索引、模板内化、文种手册、路由说明、项目规则和接手摘要，明确“锡盟/锡林郭勒盟/西乌/西乌珠穆沁旗”均优先使用该模板，不回退通用模板。

验证结果（2026-06-29 16:06 +0800）：

- `python3 skills/gongwen-drafting/scripts/gongwen-agent/internalize_word_templates.py` 已生成锡林郭勒盟西乌珠穆沁旗模板 Markdown/JSON，推断文种为“文物保护许可申请”。

## 2026-06-29 v1.4 锡林浩特市临时用地是否涉及文物请示照片模板

- 新增 `assets/templates/锡林郭勒盟/模板照片——锡林浩特市临时用地是否涉及文物请示.jpg`，作为锡林浩特市临时道路、临时车辆行驶道路、临时用地范围查询是否涉及文物的照片模板来源。
- 新增 `模板——锡林浩特市临时用地是否涉及文物请示` Markdown/JSON 内化，明确受文单位通常为“锡林浩特市文体旅游广电局”，标题采用“关于申请查询项目临时用地是否涉及文物的请示”口径，附件最小清单为“临时用地范围及电子版国家2000坐标”。
- 更新模板索引、模板内化总览、manifest、文种索引、文种手册、路由说明、项目规则、接手摘要和固定版式说明，明确“锡林浩特/锡林浩特市/锡市”均优先使用该模板，不回退通用模板；来源照片遮挡处不得猜测。

验证结果（2026-06-29 18:45 +0800）：

- 图片分析提取了照片中可识别的标题、受文单位、正文骨架、附件和样件日期；遮挡处均按可变项处理。
- 已新增路由测试，覆盖 `锡市 + 文物核查请示` 并命中本地模板；`锡林浩特市 + 查询用地是否涉及文物` 计划走通用模板时以退出码 2 拦截；JSON 校验、脚本编译、12 项单元测试、skill 自检和官方 skill 校验均通过。

## 2026-06-30 v1.5 锡林浩特模板路由补丁

- 修正 v1.4 的两个路由缺口：项目名称/材料出现“胜利矿区”“胜利煤田”“胜利矿田”且指向锡林浩特市时，归入锡林浩特市地方模板；材料不是临时用地时，不因“临时用地”字样不匹配而回退通用模板。
- 更新路由别名、模板内化、文种手册、路由说明、项目规则、接手摘要和固定版式说明，明确非临时用地应保留锡林浩特模板的受文单位、标题口径、申请核查句和附件口径，同时替换不符合事实的“临时用地”“临时车辆行驶道路”表述。
- 新增回归测试覆盖 `胜利矿区 + 文物核查请示` 应命中锡林浩特市模板。

验证结果（2026-06-30 10:10 +0800）：

- `python3 -m unittest scripts/gongwen-agent/test_template_route.py` 通过，运行 2 项。
- `胜利矿区 + 文物核查请示` 返回 `local` 并命中“锡林浩特市临时用地是否涉及文物请示”；计划走 `generic` 时以退出码 2 拦截。
- 锡林浩特模板 JSON 可正常解析。
